// TimeBridge API config
//
// IMPORTANT: "localhost" only works on iOS simulator and the web build.
// On a real phone running Expo Go you must use your computer's LAN IP, e.g.
//   export const API_URL = "http://192.168.1.42:5050";
// Find your Mac's LAN IP with:  ipconfig getifaddr en0
//
// Android emulator (not a real device): use http://10.0.2.2:5050
export const API_URL = "http://localhost:5050";
