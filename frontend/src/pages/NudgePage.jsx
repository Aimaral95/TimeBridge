/* DEPRECATED — kept as a placeholder so accidental imports still resolve.

   The "Nudge / Call requests" feature was scoped out for the v1 demo:
     - It wasn't routed anywhere (App.jsx never imported it)
     - All data on the page was hard-coded ("Mom / Dad / Sister")
     - The "Send nudge" flow had no backend endpoint

   It was removed during the senior-design polish pass.
   See docs/REPORT_OUTLINE.md → Future Work for the planned re-introduction. */

export default function NudgePage() {
  return null
}
